## Summary

<!-- What does this change do, and why? -->

## Related issue(s)

<!-- Closes #... -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] Added/updated tests for the behavior changed
- [ ] Updated `docs/domain-model.md` if this changes the domain model or permissions matrix
- [ ] Updated `prisma/schema.prisma` migrations if the DB schema changed (`npm run prisma:migrate --workspace=apps/backend`)

## Screenshots (if UI change)
